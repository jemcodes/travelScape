window.addEventListener('DOMContentLoaded', (err) => {
  const stampButton = document.getElementById("button");
  stampButton.addEventListener("click", async (err) => {
    err.preventDefault();
    const articleId = document.getElementById("")
  })
})