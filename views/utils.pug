mixin field(labelText, fieldName, fieldValue, fieldType = 'text', placeholder = null)
  div(class='form-group')
    label(for=fieldName)= labelText
    input(type=fieldType id=fieldName name=fieldName value=fieldValue class='form-control' placeholder=placeholder)

mixin validationErrorSummary(errors)
  if errors
    div(class='alert alert-danger' role='alert')
      p The following error(s) occurred:
      ul
        each error in errors
          li= error

mixin fieldNoLabel(classList, fieldName, fieldValue, fieldType = 'text', placeholder = null)
  div(class=classList)
    if fieldType === 'textarea'
      textarea(id=fieldName name=fieldName rows='10' placeholder=placeholder spellcheck=true)=fieldValue
    else 
      input(type=fieldType id=fieldName name=fieldName value=fieldValue placeholder=placeholder)